### Example environment variables for the NPS app (do NOT commit your real .env)
# Copy this file to `.env` and fill the values for local development.

# Full n8n webhook URL (example: https://example.com/webhook/survey)
VITE_N8N_WEBHOOK_URL=

# API key to use when calling your n8n webhook (will be sent as header).
# Default header name is 'x-api-key' but you can override with VITE_N8N_API_KEY_HEADER.
VITE_N8N_API_KEY=
VITE_N8N_API_KEY_HEADER=x-api-key

# Optional: set a human-readable name used in the payload
VITE_AUTOESCOLA_NAME=AutoEscola Modelo
